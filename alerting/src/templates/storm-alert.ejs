<% if (!isPlainText) { %> 
<!-- Version HTML -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>PRISM Alert</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 20px auto;
            background: #ffffff;
        }
        .header {
            text-align: center;
            padding-top: 30px;
        }
        .title {
            font-size: 20px;
            font-weight: 500;
            line-height: 23.44px;
        }
        .content-title {
            font-size: 24px;
            font-weight: 700;
            line-height: 30px;
            color: #333;
        }
        .content-subtitle {
            margin-bottom: 40px;
        }
        .content {
            padding: 50px;
            font-size:14px;
        }
        .table-container {
            width: 100%;
            border-collapse: collapse;
            font-size: 16px;
            font-weight: 700;
        }
        .table-container th, .table-container td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }
        .table-container th {
            background-color: #f8f8f8;
        }
        .button-container {
            text-align: center;
            margin: 20px 0;
        }
        .button {
            display: inline-block;
            text-align: center;
            background: rgba(99, 178, 189, 1);
            color: black;
            padding: 12px;
            text-decoration: none;
            border-radius: 5px;
            white-space: nowrap;
            
            font-weight: 600;
            
        }
        @media (max-width: 768px) {
            .button span {
                display: none; 
            }
            .button::before {
                content: "ACCESS DASHBOARD";
                display: inline;
            }
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: white;
            padding: 15px 0;
            width: 100%;
            background-color: rgba(99, 178, 189, 1);
        }
        .footer .title {
            margin-bottom: 15px;
        }
        
        .footer p {
            margin-bottom: 15px;
        }
        
        .footer a {
            color: white;
            font-weight: bold;
            margin-top: 10px;
        }
        .readiness-icon {
            width: 24px;
            height: 24px;
            border-radius: 8px;
            background-color: rgba(99, 178, 189, 1);
            display: inline-block;
            vertical-align: middle;
            margin-right: 10px;
        }
        .activation-icon {
            width: 24px;
            height: 24px;
            border-radius: 8px;
            background-color: rgba(230, 55, 1, 1);
            display: inline-block;
            vertical-align: middle;
            margin-right: 10px;
        }
        .trigger-title {
            margin: 20px 0;
            width: 100%;
            font-weight: bold;
            font-size: 16px;;
        }
        .gray-box {
            background-color: rgba(241, 241, 241, 1);
            padding: 20px;
            margin-top: 20px;
            color: black; 
        }
        
    </style>
</head>
<body>
    <div class="container" style="color:black">
        <header class="header"><p class="title">PRISM</p></header>
        <main class="content">
            <h1 class="content-title">
                <%= alertTitle %>
            </h1>
            <p class="content-subtitle">ANTICIPATORY ACTION ALERT FOR TROPICAL STORM <%= cycloneName %> - <%= cycloneTime %> - 
                FORECAST BY REGIONAL SPECIALIZED METEOROLOGICAL CENTER LA REUNION</p>
            <p class="trigger-title">
                    <span class="readiness-icon"></span> Readiness Trigger: 
                    <% if (readiness) { %>
                        Activated
                    <% } else { %>
                        Not Activated
                    <% } %>
            </p>
            <% if (readiness) { %>
                <p><%= cycloneTime %> there was 20% likelihood of Mozambique experiencing tropical storm-force winds within the next 5 days</p>
            <% } %>
            <p class="trigger-title">
                <span class="activation-icon"></span> Activation Triggers: 
                <% if (activatedTriggers) { %>
                    Activated
                <% } else { %>
                    Not Activated
                <% } %>
            </p>
                
            <% if (activatedTriggers) { %>
                <p>Projected wind speeds affecting districts in the next 72 hours:</p>
                <table class="table-container">
                    <tr>
                        <td>&gt; 89 km/h</td>
                        <td><%= activatedTriggers.districts48kt %></td>
                    </tr>
                    <tr>
                        <td>&gt; 118 km/h</td>
                        <td><%= activatedTriggers.districts64kt %></td>
                    </tr>
                </table>
            <% } %>
            <div class="button-container">
                <a href="<%= redirectUrl %>" target="_blank" rel="noopener noreferrer" aria-label="Access PRISM Tropical Storm Dashboard (opens in a new tab)" class="button">
                    <span style="vertical-align: middle;color:black">ACCESS PRISM TROPICAL STORM DASHBOARD</span>
                    <img src="cid:arrow-forward-icon" alt="" style="width: 16px; height: 16px; vertical-align: middle">
                </a>
            </div>
            
            <h3>
                <img src="cid:map-icon" alt="" style="width: 24px; height: 24px; vertical-align: middle; margin-right: 8px;">
                Trajectory Map
            </h3>
            <img src="cid:storm-image-cid" alt="Map showing the trajectory of cyclone <%= cycloneName %> over Mozambique" width="100%" style="border: none;">

            <div class="button-container">
                <a href="<%= redirectUrl %>" target="_blank" rel="noopener noreferrer" aria-label="Access PRISM Tropical Storm Dashboard (opens in a new tab)" class="button">
                    <span style="vertical-align: middle;color:black">ACCESS PRISM TROPICAL STORM DASHBOARD</span>
                    <img src="cid:arrow-forward-icon" alt="" style="width: 16px; height: 16px; vertical-align: middle">
                </a>
            </div>
            
            <p>We will continue to closely monitor the situation and provide timely updates as more information becomes available.</p>
            <p>Best regards,</p>
            <p>WFP Mozambique Country Office</p>
            
            <p class="gray-box">
                <strong>Disclaimer</strong>: please note, this is not an official forecast. For accurate and official updates, please refer to alerts issued by <strong>INAM (Mozambique Meteorological Service)</strong>.
            </p>
        </main>

        <footer class="footer">
            <p class="title">PRISM</p>
            <p>This message was automatically generated.</p>
            <p>You can <a class="link" href="<%= unsubscribeUrl %>">unsubscribe here</a></p>
        </footer>
    </div>
 
</body>
</html>
<% } else { %>
    PRISM ALERT - <%= alertTitle %>

    Anticipatory Action Alert for Tropical Storm <%= cycloneName %> - <%= cycloneTime %> 
    Forecast by Regional Specialized Meteorological Center La Reunion.

    <% if (readiness) { %>
        Readiness Trigger: Activated
    <% } else { %>
        Readiness Trigger: Not Activated
    <% } %>

    <% if (readiness) { %>
        There was a 20% likelihood of Mozambique experiencing tropical storm-force winds within the next 5 days.
    <% } %>

    <% if (activatedTriggers) { %>
        Activation Triggers: Activated

        Projected wind speeds affecting districts in the next 72 hours:
        - > 89 km/h: <%= activatedTriggers.districts48kt %>
        - > 118 km/h: <%= activatedTriggers.districts64kt %>
    <% } else { %>
        Activation Triggers: Not Activated
    <% } %>

    Access PRISM Tropical Storm Dashboard: <%= redirectUrl %>

    Map showing the trajectory of cyclone <%= cycloneName %> over Mozambique:
    (Image unavailable in plain text format)

    We will continue to monitor the situation and provide timely updates.

    Best regards,  
    WFP Mozambique Country Office

    Disclaimer: Please note, this is not an official forecast. For accurate and official updates, please refer to alerts issued by INAM (Mozambique Meteorological Service).
<% } %> 
