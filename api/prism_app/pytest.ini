[pytest]
log_cli = true
log_cli_level = INFO
markers =
    network: HTTP or remote service calls (not run in offline pytest defaults if filters are used)
    e2e: full stack smoke (Playwright + real URLs); run manually or in dedicated job
