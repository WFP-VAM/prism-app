import type { SxProps, Theme } from '@mui/material/styles';

export const dashboardLegendsContainerSx = {
  position: 'absolute',
  top: '8px',
  maxHeight: 'calc(100% - 48px)',
  overflowY: 'auto',
} satisfies SxProps<Theme>;
